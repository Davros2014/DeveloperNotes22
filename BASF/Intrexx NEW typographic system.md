I:/external/htmlroot/css/BASF_Icons_Fonts/fonts/BASF_Icons_2019
```
.Text_H1, h1 {
  font-size: clamp(3.5rem, 3vw, 5rem);
  line-height: clamp(4rem, 3vw, 6rem);
  margin-bottom: 3rem;
} 
```

```
.Text_H2, h2 {
  font-size: clamp(2.2rem, 3vw, 3rem);
  line-height: clamp(3rem, 3vw, 4rem);
  margin-bottom: 2.5rem;
}
```

```
.Text_H3, h3 {
  font-size: clamp(2.2rem, 3vw, 3rem);
  line-height: clamp(3rem, 3vw, 4rem);
  margin-bottom: 2rem;
}
```

```
.Text_H4, h4 {
  font-size: clamp(1.5rem, 3vw, 2rem);
  line-height: clamp(2.6rem, 3vw, 3.4rem);
  margin-bottom: 1.5rem;
}
```

```
.Text_H5, h5 {
    font-size: clamp(1.6rem, 3vw, 1.8rem);
    line-height: clamp(2rem, 3vw, 2.4rem);
    margin-bottom: 1.25rem;
}
```

```
.Text_Label {
  vertical-align: middle;
  color: var(--grey80);
  font-weight: 400;
  font-size: .875rem;
  line-height: 25px;
}
```

```
.Text_Standard {
  vertical-align: middle;
  color: #333;
  font-weight: bold;
  font-size: .875rem;
  line-height: 25px;
}
```

```
.metaText {
  font-size: .8125rem;
}
```

```
.Infotext, .infotext, .Infotext_Small, .infotext_small {
  font-size: .8125rem;
}
```